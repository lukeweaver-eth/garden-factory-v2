{
  "name": "garden-factory-v2",
  "version": "2.0.0",
  "description": "GardenFactory V2 — SSTORE2 template deployer architecture",
  "scripts": {
    "compile": "hardhat compile",
    "compile:size": "REPORT_SIZE=true hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:localhost": "hardhat deploy --network localhost --reset",
    "deploy:sepolia": "hardhat deploy --network sepolia",
    "deploy:mainnet": "hardhat deploy --network mainnet",
    "verify:sepolia": "hardhat deploy --network sepolia --tags Verify"
  },
  "author": "0xfff",
  "license": "MIT",
  "devDependencies": {
    "@chainlink/env-enc": "^1.0.5",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.13",
    "hardhat-contract-sizer": "^2.10.0",
    "hardhat-deploy": "^0.12.4",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^17.3.1",
    "marked": "^17.0.4",
    "solady": "^0.0.237"
  }
}
